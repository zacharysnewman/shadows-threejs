## Summary

<!-- What changes, and why. One or two sentences — the diff shows the how. -->

## Spec & plan

<!-- This project is spec-driven: docs/GAME_SPEC.md is the source of truth and
     docs/IMPLEMENTATION_PLAN.md sets the build order. Name what this PR touches, or write
     "n/a" for work outside the build order (tooling, CI, docs). -->

- **Phase:**
- **Spec sections:**

### Spec amendments

<!-- Delete this heading if docs/GAME_SPEC.md is untouched.

     The plan's rule: if a phase turns up something the spec does not answer, fix the spec
     rather than deciding it in code. So if this PR changed the spec, say what changed and
     which gap forced it — a question the spec left open, not a preference. -->

## Exit criteria

<!-- For a phase PR, the exit criteria the plan lists for that phase, each with the
     evidence that it is met. For anything else, what "done" means here. -->

| Criterion | Evidence |
| --- | --- |
|  |  |

## Verification

<!-- What was run and what it showed. Passing tests are the floor, not the whole story:
     say whether the actual app was driven, and how. Numbers beat adjectives — draw calls,
     collider counts, warning counts. -->

## Risks & follow-ups

<!-- Delete if genuinely none.

     What should a reviewer look at hardest? What was deliberately left out, and whose call
     is it to change that? Most importantly: is there anything this PR *cannot* verify
     before merge — a code path CI structurally cannot reach, a first-run deployment step —
     so that if something breaks after merging, the first place to look is already written
     down. -->
